const array1 = [15, 2, 7];
const array2 = [5];

let numeroMassimo;

// creiamo un ciclo che inserisca valori nell'array 2 come quelli dell'array 1
for (i = 0; array1.length === array2.length; i++ ) {

    numeroMassimo = prompt(`Inserisci un valore; Hai disposizione ${i} valori`);
}